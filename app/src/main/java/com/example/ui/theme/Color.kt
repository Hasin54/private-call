package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Frosted Glass Palette (mapped to existing names to ensure backwards compatibility)
val SlateDarkBlue = Color(0xFF0F1115) // Backing deep dark backdrop
val SlateSurface = Color(0x0DFFFFFF)  // Translucent white for glass (white/5)
val SlateBorder = Color(0x1AFFFFFF)   // Translucent white border (white/10)

val CyanAccent = Color(0xFFD0E4FF)
val CyanGlow = Color(0xFFA8C7FF)

val IndigoPrimary = Color(0xFFA8C7FF) // Selected active pill background
val IndigoLight = Color(0xFF2E3035)   // Neutral dark charcoal unselected button

val GreenActive = Color(0xFF10B981)   // Success active indicator
val RedReject = Color(0xFFF87171)     // Red-400 for missed/unanswered events

val TextPrimary = Color(0xFFF8FAFC)   // Slate-100 text
val TextSecondary = Color(0xFF94A3B8) // Slate-400 text
val TextOnIndigo = Color(0xFF003061)  // High contrast navy text on primary selection
